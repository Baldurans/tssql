
* maybe handle booleans correctly? If field has comment "BOOL" or "BOOLEAN", it will consider it boolean and transform?

* insert
* update

* having, groupBy on probleeme väljade kasutamisega mis on juba Response struktuuris.
* @TODO Filter out names that exist in the Result